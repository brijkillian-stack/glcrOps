# ── Development + test dependencies ──────────────────────────────────────────
# Install: pip install -r requirements-dev.txt
# For Tier 3 (SSIM visual regression), also: pip install weasyprint
# On macOS weasyprint also needs: brew install pango cairo

# ── Testing framework ─────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.24

# ── Print regression suite — Tier 1/2 ────────────────────────────────────────
# poppler-utils must also be installed at the OS level:
#   macOS:  brew install poppler
#   Ubuntu: sudo apt-get install poppler-utils
pdf2image>=1.17
Pillow>=10.0
pypdf>=4.0
scikit-image>=0.22
scipy>=1.11

# ── Print regression suite — Tier 3 (optional) ───────────────────────────────
# Uncomment to enable SSIM visual regression. Requires pango+cairo on macOS.
# weasyprint>=61.0

# ── API tier regression ───────────────────────────────────────────────────────
httpx>=0.27
fastapi>=0.111
uvicorn>=0.29

# ── PrintService stack ────────────────────────────────────────────────────────
supabase>=2.4.0
python-dotenv>=1.0.0
