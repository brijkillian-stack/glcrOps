# GLCR Forge API — FastAPI service dependencies
# Used by Render build: pip install -r forge-requirements.txt
# Does NOT include Reflex (that's for the legacy glcrops service only)

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0
supabase>=2.4.0
redis>=5.0.0
openpyxl>=3.1.0
httpx>=0.27.0
