"""ZDS Forge — FastAPI surface for the ZDS engine.

This package exposes the placement engine and renderer through a
versioned HTTP API (`/v1/...`). It is intentionally separate from the
Reflex app under `apps/zds/`, which remains the operator-facing UI.
"""
